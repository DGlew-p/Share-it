import styled from 'styled-components';

export const Styles = styled.div`
	/* .navbar {
		background-color: #222;
	}

	.navbar-brand,
	.navbar-light,
	.navbar-nav,
	.nav-link {
		color: #fff;

		&:hover {
			color: white;
		}
	} */
`;
